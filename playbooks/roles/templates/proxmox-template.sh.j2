# download the image
# wget https://cloud-images.ubuntu.com/bionic/current/bionic-server-cloudimg-amd64.img

# create a new VM with VirtIO SCSI controller
qm create 9909 --memory 2048 --net0 virtio,bridge=vmbr0 --scsihw virtio-scsi-pci

# import the downloaded disk to the local storage, attaching it as a SCSI drive
qm set 9909 --scsi0 local:0,import-from=/root/bionic-server-cloudimg-amd64.img

qm set 9909 --ide2 local:cloudinit
qm set 9909 --boot order=scsi0
qm set 9909 --serial0 socket --vga serial0
qm template 9909
